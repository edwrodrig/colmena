#include <task.hpp>
#include <QProcess>

void Task::highPriority ( void )
{
	if ( process )
	{
	//	QProcess::execute ( QString("setpriority.exe -p256 -t15 ") + QString::number( (quint64)( ( _process->pid() )->dwProcessId ) ) ) ;
	}
}
void Task::lowPriority ( void )
{
	if ( process )
	{
	//	QProcess::execute ( QString("setpriority.exe -lowest " ) + QString::number( (quint64)( ( _process->pid() )->dwProcessId ) ) ) ;
	}
}

